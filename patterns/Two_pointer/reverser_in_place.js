// Reverses the input array of characters in place using the two-pointer technique.
// pattern:swipe from end

function reverseString(s) {
  let left = 0;
  let right = s.length - 1;

  while (left < right) {
    [s[left], s[right]] = [s[right], s[left]];
    left++;
    right--;
  }
}
